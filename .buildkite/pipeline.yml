steps:
  - label: ":docker: Build & Push"
    key: build
    command: "bash ${BUILDKITE_BUILD_CHECKOUT_PATH}/scripts/build-and-push.sh"

  - label: ":rocket: Deploy to Fargate"
    key: deploy
    depends_on: build
    command: "bash ${BUILDKITE_BUILD_CHECKOUT_PATH}/scripts/deploy.sh"
